# 四土 · 阶段 8：口语复盘大脑升级（施工规格）

> 背景：阶段 4.1 的复盘是「单遍单 prompt」，有六个硬伤：输入掐断在 3000 字符、maxTokens 起步太小、
> 无分级导致长转写信息过载、无跨次记忆、复盘历史无 UI、精批模型不可单配。本阶段全部解决，并补上
> 桌面端 monologue 录音转写（server.py 代理火山，绕 CORS）。
>
> **批次总览**（供后续窗口接续）：批1=本 spec；批2=词块系统（新窗口设计，暗号「继续四土口语复盘批2：
> 词块系统」）；批3=Chrome 扩展自动抓取 AI 对话（暗号「继续四土口语复盘批3：对话抓取扩展」）；
> 批4=双轨录音，已搁置。
>
> 主会话已设计好三条 prompt（§2，**逐字照搬一字不改**，这是产品价值核心，验收会比对）、数据模型、
> 接口与 UI。builder 照此施工，汇报贴证据（尤其真实 API 的原始 JSON）不贴结论。
> **活到你为止：不许再往下转包给任何子代理。**

## 0. 开工前

- `cd ~/Documents/situ && git add -A && git commit -m "阶段8开工前存档"`（安全绳）。
- 改动范围：`santu_app/mobile/js/core/llm.js`、`js/core/localapi.js`、`js/core/store.js`、
  `review.js`、`settings.js`、`style.css`、`santu_app/server.py`。**不碰**阅读/讲解/生词本逻辑、
  桌面 `app.py`/`index.html`。外科手术，只动复盘相关。
- 两个 mobile 目录：`santu_app/mobile`=源头（改这里）；`四土app/assets/mobile`=APK 资源（最后 rsync）。

## 1. 管线总设计

`review_speech` 内部按输入长度分流（词数按 `split(/\s+/)` 计）：

- **≤350 词：单遍**。一次调用 `REVIEW_V2_SINGLE`，直接得到 v2 结果（无 segments）。
- **>350 词：两遍**。
  1. **检出遍**：把 transcript 按句子边界切成 ~800–1100 词的块（在句号/问号/换行处切，绝不切断句子），
     每块并行调 `REVIEW_V2_DETECT`（`Promise.all`），得到各块 findings 合集。某块失败重试一次，仍失败
     则跳过该块并在返回值里带 `warnings:["第N段检出失败"]`（UI toast 提示，不整体失败）。
  2. **编辑遍**：全文 transcript + 合并后的 findings 清单 + 近期错题清单，调 `REVIEW_V2_EDIT`，
     得到 topic/overall/strengths/segments/priority/minor。
- **跨次记忆**：调用前取错题本未掌握条目按 `lastSeen` 倒序前 20 条，编号 M1…M20，格式
  `M3. {original} → {correction}（{type}）`，拼进 SINGLE / EDIT 的 `{mistakes_block}`。空错题本则该块为空串。
- **删除 `text.slice(0, 3000)`**（llm.js 现 433 行附近）——长度由分块机制兜住，总上限放宽到 12000 词
  （超出截断并 warnings 提示）。
- maxTokens：SINGLE 3072（空答重试 6144）；DETECT 每块 2048（重试 4096）；EDIT 4096（重试 8192）。
  沿用现有 `_chatWithFallback` 的空答重试模式（v4-pro 推理模型坑）。temperature 均 0.3。

## 2. 三条 Prompt（逐字照搬，一字不改）

放进 `js/core/llm.js`，命名 `REVIEW_V2_SINGLE_SYSTEM` / `REVIEW_V2_DETECT_SYSTEM` /
`REVIEW_V2_EDIT_SYSTEM` 及对应 TEMPLATE，风格与现有导出一致。

### REVIEW_V2_SINGLE_SYSTEM

```
你是一位资深的英语口语外教，专门帮"中文母语"的学习者复盘他本人产出的英文（口播录音转写或和 AI 的对话转写，可能带口语的不完整、重复、口头语，这些口语特征不要苛责）。你的任务：把最值得他记住的问题挑出来讲透，而不是把所有毛病平铺罗列。

你只能输出一个 JSON 对象（不要任何额外文字，不要 markdown 代码块）：
{
  "topic": "用 4-8 个中文字概括这段在聊什么，如：聊AI对就业的影响",
  "overall": "两三句中文总评：先一句整体印象（具体、不客套），再点出今天最值得记住的一件事",
  "strengths": ["具体夸到某个词/表达，最多 3 条；没有就空数组"],
  "priority": [
    {
      "original": "原文里出问题的最小连续片段，逐字照抄",
      "correction": "改正后的说法",
      "type": "grammar | wordchoice | collocation | naturalness 四选一",
      "why": "两句以内中文：为什么错/为什么不自然，把规则或语感差别讲清",
      "count": 1,
      "repeatOf": null
    }
  ],
  "minor": [ 与 priority 同结构的对象 ]
}

判定要求：
- priority 是"今天最值得记住的"，最多 5 条、宁缺毋滥，按此优先：①真正的语法/用词/搭配错误 ②全文反复出现的问题 ③【近期错题】里再次犯的 ④一改就明显更地道的表达。
- minor 放其余真实存在但优先级低的问题，按重要性排序。
- 同一个知识点的重复错误必须合并成一条：count 记它在全文出现的总次数，original 取最典型的一处，绝不一条条罗列。
- type=naturalness 表示没有语法错、但母语者一般不这么说；correction 填更自然的说法。
- original 必须是原文里"逐字、连续"出现的片段（系统靠它在原文里高亮定位）：原样照抄，不要改写、不要翻译、不要加引号或省略号。
- repeatOf：若该条与【近期错题】中某条是同一个知识点（同一条规则、同一个搭配），填那条的编号字符串如 "M3"；否则填 null。没有给你错题清单时一律 null。
- 口语填充词（you know / like）、轻微重复、可接受的口语化表达，不要挑。绝不为了凑数硬造问题：这段英文本来不错，priority / minor 就少给或给空数组。
- strengths 要具体（夸到具体的词或表达），不要泛泛说"整体不错"。
- 所有中文解释简短、具体、不客套。
```

### REVIEW_V2_SINGLE_TEMPLATE

```
【学习者产出的英文】
{transcript}
{context_block}
{mistakes_block}
请严格按系统要求复盘，只输出那个 JSON 对象。
```

- `{context_block}`：有背景时 `\n\n【背景 / 对方说了什么 / 话题】\n{context}`，否则空串。
- `{mistakes_block}`：有未掌握错题时 `\n\n【近期错题（未掌握，供判断是否重犯）】\nM1. ...\nM2. ...`，否则空串。

### REVIEW_V2_DETECT_SYSTEM

```
你是一位资深的英语口语外教，正在帮"中文母语"的学习者复盘他本人产出的一大段英文（口播或对话的转写，带口语特征不要苛责）。下面给你的是其中第 {i}/{n} 段。你的任务是"地毯式检出"：把这一段里所有真实存在的问题找全，交给下一环节去排优先级和整理——这一步宁可多报、不要漏报，但每一条都必须是真问题，绝不硬造。

你只能输出一个 JSON 对象（不要任何额外文字，不要 markdown 代码块）：
{
  "strengths": ["这一段里具体用得好的词/表达，最多 2 条；没有就空数组"],
  "findings": [
    {
      "original": "原文里出问题的最小连续片段，逐字照抄",
      "correction": "改正后的说法",
      "type": "grammar | wordchoice | collocation | naturalness 四选一",
      "why": "一句中文说清为什么"
    }
  ]
}

判定要求：
- 找全所有：语法错、用错词、搭配错，以及"没有语法错、但母语者一般不这么说"的表达（type=naturalness，correction 填更自然的说法）。
- original 必须是这一段原文里"逐字、连续"出现的片段：原样照抄，不要改写、不要加引号或省略号。
- 口语填充词（you know / like）、轻微重复、可接受的口语化表达，不要挑。
- why 一句话即可，详细讲解是下一环节的事。
```

### REVIEW_V2_DETECT_TEMPLATE

```
【转写第 {i}/{n} 段】
{chunk}

请严格按系统要求检出，只输出那个 JSON 对象。
```

（`{i}/{n}` 同时替换 SYSTEM 与 TEMPLATE 中的占位。）

### REVIEW_V2_EDIT_SYSTEM

```
你是一位资深的英语口语外教兼编辑。学习者说了一大段英文（完整转写附后），检出环节已把全部问题逐段找出（原始清单附后，可能有重复、有跨段的同类错误、也可能混入个别误报）。你的任务是像编辑一样把它整理成一份"能消化"的复盘：挑重点、合并同类、按话题分段，而不是平铺罗列。

你只能输出一个 JSON 对象（不要任何额外文字，不要 markdown 代码块）：
{
  "topic": "用 4-8 个中文字概括整段在聊什么",
  "overall": "两三句中文总评：先一句整体印象（具体、不客套），再点出今天最值得记住的一件事",
  "strengths": ["从各段亮点里挑最值得夸的，最多 3 条"],
  "segments": ["按内容把这次输出分成 2-6 个话题段，每段一个 4-10 字中文标题，按原文顺序排列"],
  "priority": [
    {
      "original": "逐字照抄检出清单里某条的 original（系统靠它定位，不许改写、不许新造）",
      "correction": "改正后的说法",
      "type": "grammar | wordchoice | collocation | naturalness",
      "why": "两句以内中文，把规则/语感差别讲透（可以比检出清单里的更详细）",
      "count": 1,
      "seg": "该条主要出现在哪个话题段，逐字用 segments 里的标题",
      "repeatOf": null
    }
  ],
  "minor": [ 与 priority 同结构 ]
}

整理要求：
- priority 是"今天最值得记住的"，最多 5 条、宁缺毋滥，按此优先：①真正的语法/用词/搭配错误 ②反复出现的问题 ③【近期错题】里再次犯的 ④一改就明显更地道的表达。
- 同一个知识点的多条检出必须合并成一条：count 记总次数，original 取最典型的一处，绝不一条条罗列。
- minor 放其余问题，按重要性排序，同样先合并同类。检出清单里明显误报的（其实没错的）直接丢弃。
- 每条的 original 必须逐字来自检出清单，correction / why 可以在检出的基础上改得更好。
- repeatOf：若该条与【近期错题】中某条是同一个知识点，填那条的编号字符串如 "M3"；否则填 null。
- 所有中文简短、具体、不客套。
```

### REVIEW_V2_EDIT_TEMPLATE

```
【完整转写】
{transcript}
{context_block}
{mistakes_block}

【检出清单（逐段合并）】
{findings_block}

请严格按系统要求整理，只输出那个 JSON 对象。
```

- `{findings_block}`：每行 `- [{type}] {original} → {correction}（{why}）`；各块 strengths 合并附在末尾
  `亮点候选：...`（每行一条）。

## 3. llm.js：`reviewSpeechV2(args)`

- 签名 `reviewSpeechV2({ text, context, mistakes }) -> { ok, topic, overall, strengths, segments,
  priority, minor, model, warnings?, error? }`，`mistakes` = 上层传入的 `[{id, original, correction, type}]`
  （编号顺序即 M1…Mn，返回的 repeatOf 由上层映射回 id）。
- 结构镜像现有 `reviewSpeech`：同 `_loadConfig` 思路（见 §6 精批模型档）、同 `_extractJson` 容错、
  同空答重试。
- 校验：priority/minor 条目缺 original/correction/type/why 任一则丢弃该条；count 强制为 ≥1 整数；
  seg 不在 segments 里则置空（UI 归入"其他"）；repeatOf 不匹配 `M\d+` 则置 null；priority 超 5 条时
  截取前 5、余下并入 minor 头部。
- 旧 `reviewSpeech` 保留不删（还有旧调用方时不破坏），`localapi.review_speech` 切到 V2。

## 4. store.js / localapi.js

- **reviews 表**（bump DB version，onupgradeneeded 兼容旧行）：新行写入
  `{ id, createdAt, transcript, context?, source, model, version:2,
     result:{topic, overall, strengths, segments, priority, minor}, mistakeIds }`。
  旧行（无 version 字段）保持可读。
- **mistakes 表**：加字段 `recurCount`（默认 1）。
- **`review_speech` v2 内部**：
  1. 取未掌握错题前 20 条传给 `reviewSpeechV2`；
  2. 结果里 `repeatOf` 命中的条目：**不新建** mistakes，找到对应旧条目 `recurCount+=1`、
     `lastSeen=now`，结果条目带回 `mistakeId=旧id` 和 `recur:recurCount`；
  3. 其余 **priority 条目自动建 mistakes**（severity 按 type：naturalness→naturalness，其余→error）；
     **minor 条目不自动入库**，返回值里 `mistakeId:null`；
  4. 写 reviews 一条（含 topic），返回完整 v2 结果 + reviewId。
- **新方法**：
  - `save_mistake_from_item({ original, correction, type, why, reviewId }) -> {ok, mistakeId}`
    （minor 条目"入错题本"按钮用；同 original+correction 已存在则直接返回旧 id，不重复建）。
  - `list_reviews({ q? })` 升级：返回 `{ id, createdAt, topic, snippet, source, wordCount,
    priorityCount, minorCount }[]` 倒序；`q` 同时搜 topic 和 transcript（不区分大小写）。
  - `get_review({ id }) -> 完整 review 行`（历史屏重开用）。
- **高亮匹配鲁棒化**（review.js `_highlightTranscript`）：`indexOf` 找不到时，做一次宽松匹配——
  两边都按「小写化 + 把连续空白折成单空格 + 去掉首尾标点」规范化后再定位映射回原文；再找不到才放弃
  高亮（条目仍列出，不报错）。

## 5. UI：结果屏 v2 + 历史屏（review.js + style.css）

配色沿用现有：暖纸 `#faf8f4`、深蓝 `#16335c`、赭橙(error)/暖金(naturalness)/橄榄绿(strengths)。
**不新增色**。中文霞鹜文楷、英文衬线，照现有。

### 结果屏 v2（从上到下）
1. meta 行（词数 · 时间 · 模型 · topic）。
2. **总评卡**：overall，深蓝字、显眼但不花哨。
3. **原文卡**：默认折叠只露约 4 行 +「展开全文」；高亮逻辑沿用（priority+minor 都参与高亮，
   error 类赭橙、naturalness 类暖金）。
4. **重点区**：priority 卡片（复用现有 rev-card 样式），每张卡新增：`count>1` 显示「×N」小徽章；
   `recur` 存在显示「⚠️ 第 {recur} 次」徽章（赭橙）；已自动入错题本的显示现有"已进错题本"标。
5. **用得好**：strengths（现有样式）。
6. **完整清单**（仅当有 minor 或 segments）：按 segments 顺序分块，每块一个可折叠的话题段
   （标题 = seg 名 + 该段条数），默认折叠；块内列该段的 minor 条目卡片，每张带「入错题本」小按钮
   （点击调 `save_mistake_from_item`，成功后变"已进错题本"）。seg 为空的归入「其他」块。
   短文本（无 segments）就一个平铺的「次要」折叠块。
7. 底栏沿用：「N 条已存进错题本」（=priority 实存数）+「错题本」+「练这几条」（=priority 的 mistakeId 们）。

### 历史屏
- 入口：复盘头部加一个「历史」按钮（细线 SVG 时钟图标，风格照 `_RS` 现有图标），与错题本按钮并列；
  `_onBack` 路由相应补 `history` 分支。
- 列表：顶部搜索框（搜 topic/原文，防抖 280ms 照现有）；按日分组（日期头「7月6日 · 周一」），
  每条显示：topic（无则 snippet）、时间、来源图标（🎤/键盘，用细线 SVG 别用 emoji）、词数、
  「重点 N · 次要 M」。
- 点条目 → `get_review` → 用结果屏渲染函数重开（v2 行直接渲染；旧 v1 行降级渲染成原三栏样式，
  写个小分支即可，别报错）。

## 6. 设置：精批模型档（settings.js + llm.js）

- 设置屏新增「复盘精批模型」卡：provider（同现有选项）/ model / API Key / Base URL 四项，
  全部留空 = 跟随主配置。存 IndexedDB settings（键 `review_provider` / `review_model` /
  `review_api_key` / `review_base_url`）。
- llm.js 加 `_loadReviewConfig()`：先读 review_* 键，缺哪项回落主配置对应项；`reviewSpeechV2`
  用它。其他 LLM 功能不受影响。

## 7. 桌面 monologue：转写代理 + 录音升级

- **server.py**：新增 `POST /api/transcribe_submit` 与 `POST /api/transcribe_query`——纯转发代理：
  请求体带 `{appid, token, ...原火山参数}`，server 端拼火山 headers 转发到
  `https://openspeech.bytedance.com/api/v3/auc/bigmodel/submit|query`，原样回传 body。
  凭证只经手不落盘。火山协议细节**逐字照** `HANDOFF-手机原生-④语音训练.md` §★（resource-id
  `volc.seedasr.auc`、reqid 复用、靠 body 判完成态），别再查文档。照 server.py 现有 handler 风格写。
- **localapi.js `transcribe_audio`**：保持现有原生桥分支；无原生桥（桌面浏览器）时走
  `fetch('/api/transcribe_submit'|'_query')` 同样的 submit+轮询逻辑。凭证仍读 IndexedDB
  `volc_appid`/`volc_token`（桌面浏览器设置屏同样能填）。
- **review.js 录音升级**：
  - 录音时**边录边降采样转 16k Int16** 存块（替掉现在整段 Float32 攒内存的做法，长录不爆内存）；
  - 60 秒上限改为：**每满 10 分钟自动分段**——后台把该段编 WAV→转写→文字追加进 textarea，
    录音不中断继续下一段；总上限 60 分钟。UI 上录音计时照旧，分段转写静默进行，某段转写失败
    toast 提示但不打断录音。
  - 火山对 10 分钟 wav（约 19MB，base64 约 26MB）能否接受**要实测**（Step 4），不行就降到 5 分钟分段。

## 8. 分步施工 + 验收（每步贴证据）

- **Step 1 · 大脑先通（先报这步）**：llm.js 三条 prompt + `reviewSpeechV2` + 分块逻辑 +
  localapi/store 改动。写 `_devtest/test_review_v2.mjs`（仿现有 `_devtest/test_review.mjs`），对真实
  DeepSeek 跑三组并**贴原始 JSON 全文**：
  - (a) 短文本有错段（用 4.1 那段 Yesterday I go to the park…）→ 走单遍，看 priority 挑得对不对、
    topic/overall 像不像样；
  - (b) 长文本 ≥600 词（自拟一段带重复错误的口语转写，比如把同一个时态错重复 3 处）→ 走两遍，
    看分块、合并（count≥2 至少一条）、segments 是否 2-6 个且标题合理；
  - (c) 传入伪造错题清单 M1…M3，其中一条故意与文中错误同知识点 → 看 repeatOf 是否命中。
- **Step 2 · UI**：结果屏 v2 + 历史屏 + 设置卡。`node --check` 全部改动文件 + `npm run build`
  （core.bundle 重打）成功。
- **Step 3 · preview 闭环（桌面浏览器）**：起 server.py → 开复盘 → 跑 (a)(b) 两段真调用 →
  验证：总评/重点/折叠段渲染、×N 与重犯徽章、minor「入错题本」、历史屏分组+搜索+重开旧复盘
  （含一条 4.1 时代的 v1 旧数据不崩）、精批模型卡留空时走主配置。贴：结果屏与历史屏截图 +
  console 零报错日志。
- **Step 4 · 桌面录音转写**：preview 里录一段 ≥15 秒真麦克风 → 走 server 代理转写成功贴回文字；
  再验 10 分钟分段逻辑（可把分段阈值临时调成 30 秒来验流程，验完改回 10 分钟，并在汇报里注明
  10 分钟真实档位未实测长录）。
- **Step 5 · 同步 + 收尾**：`npm run build` → rsync 到 `四土app/assets/mobile/`（命令照
  `HANDOFF-手机原生-④语音训练.md` §构建）→ `flutter build apk`（arm64 release）→ 拷
  `~/Desktop/四土手机版/四土.apk`（不用 adb 装，用户自装）。更新 `santu_app/mobile/CHANGELOG.md` +
  同步中央改动日志库（`~/Documents/改动日志库/四土/` 概述+详细）。git commit。
  一句话汇报：大脑通否、UI 通否、prompt 实测质量观感、未决问题。

## 9. 红线

- **三条 prompt 逐字照搬 §2**，一字不改（主会话验收比对）。
- 外科手术：不碰阅读/讲解/生词本/写作训练既有逻辑；`check_writing`/`make_writing_drill` 原样保留。
- 密钥永不进代码：火山与 LLM 凭证全走 IndexedDB/请求体，server 代理不落盘不打日志。
- v4-pro maxTokens 照 §1 给足 + 空答重试。
- 错题本/复盘数据全本地，不上传。
- 别删原有 `reviewSpeech`（v1）与旧 REVIEW_SYSTEM 常量。
