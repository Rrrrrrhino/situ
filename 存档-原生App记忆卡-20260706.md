# 存档：四土原生 App 记忆状态卡（2026-07-06 记忆瘦身撤回）

> 原为记忆卡 project_situ_native_app 全文，记录 2026-06-29 → 2026-07-01 原生安卓 App 从立项到四大件真机验收的全程。当前接续以 `HANDOFF.md` 与 `specs/手机原生app-施工规格.md` 为准。

四土 2026-06-29 起转做**真·独立安卓 App**：用户要"Mac 关机、出门流量也能读"的真 App，接受装 Android SDK、放弃实时桌面同步（改手动导出导入）。

**架构（已定）**：复用现成手机网页 UI `santu_app/mobile/`，把 Python 后端"大脑"端口成 WebView 内跑的纯 JS，打包成 Flutter WebView 安卓 APK；外部请求（DeepSeek/MiniMax/抓网页）走 Dart↔JS 原生 HTTP 桥绕 CORS，其余（拆 EPUB、词汇分层、渲染、存储）全在 JS 本地跑，数据存 IndexedDB。唯一耦合点是 app.js 的 `api()`，换成本地 LocalApi 分发即可，UI 不动。

**关键选型**：spaCy 用 **wink-nlp + wink-eng-lite-web-model**（4.6MB）替代成功——不规则词形 mice→mouse 等全对、821 词 8ms。词表 cet4/cet6/subtlex 打包 JSON。LLM prompt 逐字端口（默认 deepseek-v4-pro，注意 reasoning max_tokens 陷阱）。

**进度（2026-06-29）**：阶段 0/1A/1B/1C/1D 全部完成并验收——后端"大脑"全部端口成浏览器内 JS（`mobile/js/core/`，esbuild→core.bundle.js）。1C 数据层真 EPUB 端到端（book/22章/97高亮/存档恢复）+6 本解析+store19/19；1D AI 层真 DeepSeek 验讲解/选区/追问/有道发音。

**① 阶段 2 打包 APK**：规格 `specs/阶段2-打包APK-施工规格.md`——选型 flutter_inappwebview（InAppLocalhostServer 伺服 assets+稳定 IndexedDB 源、callHandler 返回 Promise 接 NativeHttp 桥），以 yulian Flutter 工程为构建蓝本。**② handoff 澄清**：原生 App 接续以总规格为准；`HANDOFF-手机版.md` 是旧 PWA 路线已加废弃横幅。**③ 无数据线也能装**：apk 用微信文件传输助手发自己。

**阶段 4「口语复盘」**（2026-06-29 定）：录我说的英文→AI 复盘（哪好/哪错/换种更自然）→错题本→训练。闭环 90% 复用现有 JS 大脑；ASR=火山引擎。只可靠录"我自己的声音"（安卓录豆包音频易被挡+戴耳机物理收不到，已对用户讲清）。分步 4.1 复盘大脑→4.2 错题本+训练→4.3 录音+ASR。
- **⑤ 4.1 复盘大脑已完成 CC 亲验**：规格 `specs/阶段4.1-口语复盘大脑-施工规格.md`；llm.js REVIEW_SYSTEM/reviewSpeech、store DB v2 加 reviews/mistakes、localapi review_speech+错题本 CRUD、review.js 三屏 UI。浏览器直连 DeepSeek CORS 通。key 走 IndexedDB 设置。
- **⑥ APK 交付教训**：152MB debug 包在华为鸿蒙"打不开"（debug 被当测试版拦截）；改 release + split arm64（18.7MB，apksigner v2）。**交付一律放 `~/Desktop/四土手机版/`**（apk+试用 command+排错 txt），别埋 build 目录。装前先卸旧版（签名冲突）。火山 ASR 用「豆包语音识别」开通即送免费额度。
- **⑦ 4.2 写作训练已完成 CC 亲验**：规格 `specs/阶段4.2-错题本写作训练-施工规格.md`；llm.checkWriting+store DB v3 trainings 表；错题本多选→训练屏→批改结果屏（✓用对/✗用错/—没用上+quote 高亮+中文反馈+总评+标掌握）。preview 坑：截图偶因 viewport 塌成 10px 变空白→preview_resize mobile 复原。
- **⑧ 曾误判"鸿蒙跑不了 Flutter"暂停**（连裸 Flutter app 都首帧前闪退）→ **⑨ 被推翻（2026-07-01）**：adb logcat 抓到真栈——两个配置 bug：①包名不匹配（MainActivity.kt package `com.situ.situ_reader` vs applicationId `com.situ.reader`→ClassNotFoundException）②明文被拦（InAppLocalhostServer http://localhost 安卓9+禁明文→Manifest 加 `android:usesCleartextTraffic="true"`）。**教训：原生启动崩溃别猜"引擎/系统不兼容"，第一步永远是插线 `adb logcat -b crash` 抓真栈**——两个 5 分钟能修的 bug 曾被误判成死路搁置两天。构建环境：`JAVA_HOME=/opt/homebrew/opt/openjdk@17`+`ANDROID_HOME=/opt/homebrew/share/android-commandlinetools`+flutter。
- **⑩ 网址阅读**（2026-07-01 真机验通）：localapi process() URL 分支原是占位桩；正文提取早已内置（@mozilla/readability 的 extractFromHtml）；缺的是原生桥 GET——main.dart 加 NativeHttp.get（带 Safari UA，CNA 拒非浏览器请求）+http.js httpGet+localapi 真流程。**release 包 WebView console 的 debugPrint 被 strip→adb logcat 看不到 JS console，靠截图验。**
- **⑪ 读物精选（RSS）+两目录分叉解决+口语复盘进包**（2026-07-01 真机验）：应用内选媒体站→近期文章列表→点进阅读；走官方 RSS（CNA `rssfeeds/8395986`、The Conversation `articles.atom`）；规格 `specs/阶段5-应用内读物精选-施工规格.md`；localapi fetch_feed（**RSS 取 `<link>` 文本 / Atom 取 link[rel=alternate] 的 href 属性，两分支取法不同是最易错点**）+discover.js。**`santu_app/mobile`（源头）vs `四土app/assets/mobile`（APK 打包用）曾分叉**——用 `rsync --delete`（排除 node_modules/.claude/_devtest/package*）全量同步 + pubspec.yaml assets 加 review.js/discover.js（独立 script 必须列入否则伺服不到）。**adb 华为息屏即掉线，长构建后需用户重新点亮+切"传输文件"。**
- **⑫ 图标对齐桌面（emoji→细线SVG）**（2026-07-01 真机验）：规格 `specs/阶段6-图标对齐桌面细线SVG-施工规格.md`；改 5 个非 bundle UI 文件不需 npm build；坑：app.js sayBtn `btn.textContent='🔊'` 会清掉 SVG→改 innerHTML+纯 class 切换。
- **⑬ 阶段7 语音训练（录音→转写→灌口语复盘）**（2026-07-01 真机验收）：凭证=「豆包录音文件识别模型2.0-标准版」（App ID 6295788803）。**CC 用真凭证 curl 逐个探测取证**：①grant 实际映射 resource-id **`volc.seedasr.auc`**（文档写的 bigasr.auc 全报 45000030）②收 base64 wav（手机无需公网 url）③异步两步 `POST openspeech.bytedance.com/api/v3/auc/bigmodel/{submit,query}`，query 用同一 Request-Id、**不需 X-Tt-Logid**（原生桥拿不到响应头）④JS 靠 body 判态（完成=audio_info.duration 出现）。规格 `specs/阶段7-语音训练-施工规格.md`；Manifest RECORD_AUDIO+onPermissionRequest 授 WebView 麦克风；localapi transcribe_audio（凭证读 IndexedDB volc_appid/volc_token；**改 core 需 npm run build 重打 bundle**）；review.js 🎤 录音=Web Audio 自录（getUserMedia+ScriptProcessor→16k 单声道 16-bit WAV→base64；**不用 MediaRecorder**——安卓 WebView 出 webm/opus 火山不认）+60s 自动停。华为 WebView getUserMedia 实测能录。**教训：接第三方 API 先拿用户真凭证 curl 探测（resource-id 4 候选一把梭），比读 SPA 文档靠谱。**

**至此 ①读物精选②口语复盘/写作训练进包③图标对齐④录音转写 四大件全部真机验收，手机功能追平并超过电脑版，项目主体告一段落（2026-07-01）。**
